from PIL import Image
from pylab import *

# 1. Загружаем изображение
# Убедись, что файл img2.jpg находится в той же папке
im = array(Image.open("img2.jpg"))
imshow(im)

# 2. Определяем координаты точек
x = [100, 100, 400, 400]
y = [200, 500, 200, 500]

# --- Рисуем всё сразу ---

# Красные звездочки во всех четырех точках
# plot(x, y, 'r*', markersize=10)

# Синяя сплошная линия (только между первыми двумя точками)
# plot(x[:2], y[:2], 'b-')

# Зеленая линия с круглыми маркерами (соединяет все точки по порядку)
# Чтобы она не перекрывала черную, сместим её чуть-чуть (необязательно)
# plot([i + 20 for i in x], y, 'go-')

# Черная пунктирная линия с квадратными маркерами
plot([i + 40 for i in x], y, 'ks:')

# 3. Настройка
title('Plotting: "all elements"')
axis('off') # Скрыть оси координат

# 4. Показать результат
show()